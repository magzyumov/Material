package ru.magzyumov.material.common

interface Constants {
    interface Common {
        companion object {
            const val SETTINGS = "settings"
            const val THEME = "theme"
        }
    }
}